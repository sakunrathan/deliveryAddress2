import { CustomDisabledDirective } from './custom-disabled.directive';

describe('CustomDisabledDirective', () => {
  it('should create an instance', () => {
    const directive = CustomDisabledDirective;
    expect(directive).toBeTruthy();
  });
});
